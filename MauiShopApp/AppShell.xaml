<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiShopApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiShopApp"
    xmlns:views="clr-namespace:MauiShopApp.View"
    Shell.FlyoutBehavior="Disabled"
    >

    <TabBar>
        <ShellContent Title="User"
                     Icon="user"
                     ContentTemplate="{DataTemplate views:UserPage}" />
        <ShellContent Title="Home"
                     Icon="home"
                     ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Basket"
                     Icon="basket"
                     ContentTemplate="{DataTemplate views:BasketPage}" />      
        <ShellContent Title="Logger"
                     Icon="transfer"
                     ContentTemplate="{DataTemplate views:TransferLoggingPage}" />    
    </TabBar>

</Shell>
