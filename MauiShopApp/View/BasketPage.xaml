<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage x:Class="MauiShopApp.View.BasketPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiShopApp.View"
             xmlns:pages="clr-namespace:MauiShopApp.ViewModel"
             xmlns:cl="clr-namespace:MauiShopApp.Layouts"
             Title="{Binding Title}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="90" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <BoxView Grid.Row="0"
                 Grid.Column="1"
                 Color="Purple" />
        <Label Grid.Row="0"
               Grid.Column="1"
               Text="Row1, Column 1"
               HorizontalOptions="Center"
               VerticalOptions="Center" />

        <VerticalStackLayout Grid.Row="1" Grid.ColumnSpan="1" >
            <Label HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Text="test"></Label>
        </VerticalStackLayout>
    </Grid>

    <!--<ScrollView BackgroundColor="#f5f8fd">


    </ScrollView>-->

</ContentPage>